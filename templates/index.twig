<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ siteName }}</title>
    <link rel="stylesheet" href="{{ url('assets/css/style.css') }}"> <!-- Link to your compiled CSS -->
</head>
<body>
{% include '_header.twig' %}

<main>
    <div class="banner">
        <img src="{{ url('assets/images/banner.jpg') }}" alt="Banner foto">
        <div class="banner-content">
            <h1>Welkom bij SuperCar's By Dario!</h1>
            <a href="{{ url('overzicht') }}" class="btn">Ontdek meer</a>
        </div>
    </div>

    <div class="popular-products">
        <h1 class="title">Aanbevolen producten</h1>
        <div class="product-list">
            {# Commented out for example purposes #}
            {# {% for product in products %} #}
            {#     <div class="product-card"> #}
            {#         <img src="{{ product.image }}" alt="{{ product.title }}"> #}
            {#         <h3>{{ product.title }}</h3> #}
            {#         <p>{{ product.description }}</p> #}
            {#     </div> #}
            {# {% endfor %} #}
        </div>
    </div>

    <div class="flex">
        <a href="{{ url('overzicht') }}">
            <button class="btn">Bekijk alles</button>
        </a>
    </div>
</main>

{% include '_footer.twig' %}

<script src="{{ url('assets/js/script.js') }}"></script>
</body>
</html>
